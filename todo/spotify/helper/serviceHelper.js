/*
const enchodeParams = params => {
    const encode = item => encodeUTI
}

module.exports.getData = function getData(url, params) {
    if (params) {
        url = `${url}?${params}`;
    }
    return fetch(url,getOptions("GET"));
}
*/
/*
module.exports.postData = function getData(url, params) {
    if (params) {
        url = `${url}?${enchodeParams(params)}`;
    }
    return fetch(url,getOptions("GET"));
}
*/